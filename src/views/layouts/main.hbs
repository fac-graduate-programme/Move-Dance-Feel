<!DOCTYPE html>
<html lang="en">
{{>head}}

<body>
  <main>
      {{>navbar}}
    {{>header}}
    {{{body}}}
  </main>
  {{>footer}}
  {{#each js}}
  <script src="js/{{this}}.js"></script>
  {{/each}}
</body>

</html>